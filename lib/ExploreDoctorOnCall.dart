import 'package:flutter/material.dart';

class ExploreDoctorOnCall extends StatelessWidget {
  const ExploreDoctorOnCall({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text(
        "Explore DoctorOnCall",
        style: TextStyle(fontWeight: FontWeight.bold, fontSize: 17),
      )),   
    );
  }
}
